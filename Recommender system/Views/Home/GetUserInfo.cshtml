@model Recommender_system.Models.UserInfoModel
@{
                /**/

                Layout = null; 
}
<a href="#" class="dropdown-toggle" data-toggle="dropdown">
    <span class="username">@Model.Emri <i class="ti-angle-down"></i> </span>
</a>
<ul class="dropdown-menu dropdown-dark">
    <li>
        @Html.ActionLink("Profili", "Profili", "Manage", routeValues: new { id = Model.ID }, htmlAttributes: new { title = "Manage" })
       
    </li> 
    <li>
        @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" }))
        {
            @Html.AntiForgeryToken()
            <a href="javascript:document.getElementById('logoutForm').submit()">Log out</a>
        } 
    </li>
</ul>
 